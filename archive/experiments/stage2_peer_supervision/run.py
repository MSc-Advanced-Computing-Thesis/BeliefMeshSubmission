# Stage 2: Peer supervision sufficiency. Experiment Specification Sec 8.
#
# Node A fine-tunes on red-filtered inputs against true angles. Node B trains
# only on Node A's predictions, sees red-filtered inputs. Also run the
# simultaneous-training variant (both nodes adapting at once).
