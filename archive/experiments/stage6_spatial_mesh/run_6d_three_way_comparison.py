# Stage 6D: Three-way comparison. Experiment Specification Sec 8 & Sec 9 step 3.
#
# The central empirical result. Frozen pretrained baseline, naive aggregation,
# and Bayesian fusion belief exchange run in lockstep on the highly dynamic
# environment with identical wearable positions and identical fresh pretrained
# weight initialisation. This is what Stage 6D should have run from the start
# (Spec Sec 9 step 3) -- the prior result used certainty-weighted scalar
# averaging in the "fusion" arm, not true product-of-experts fusion.
